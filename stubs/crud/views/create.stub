@extends('backend.layouts.app')

@section('title', __('Create {{modelName}}'))

@section('content')
    <div class="mb-6">
        <h1 class="text-2xl font-bold">{{ __('Create {{modelName}}') }}</h1>
    </div>
    <form action="{{ route('{{resourceName}}.store') }}" method="POST" class="bg-white dark:bg-gray-900 rounded shadow p-6">
        @csrf
        {{fields}}
        <button type="submit" class="btn btn-primary">{{ __('Save') }}</button>
    </form>
@endsection